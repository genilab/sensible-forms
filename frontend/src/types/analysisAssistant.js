/**
 * analysisAssistant.js
 *
 * Defines shared data shapes used by the Analysis Assistant domain.
 *
 * These structures should match backend request and response schemas.
 *
 * This improves consistency and reduces integration errors.
 */

// Example Code:

/** @typedef {{ data_summary: string }} AnalysisRequest */
/** @typedef {{ insights: string }} AnalysisResponse */

export {};
