[pytest]
testpaths =
    backend/app/tests

# This branch intentionally excludes tests for the `form_deployment` and
# `question_generation` domains from the default test run.
addopts = -k "not form_deployment and not question_generation"
